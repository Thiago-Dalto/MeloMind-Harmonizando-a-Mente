document.getElementById('login-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Aqui você pode adicionar o código para enviar os dados do formulário para a autenticação
});

document.getElementById('register-btn').addEventListener('click', function() {
    // Aqui você pode adicionar o código para redirecionar para a página de registro
    alert('Redirecionar para a página de registro');
});
